<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/lightWhite"
                android:centerColor="@color/lightWhite"
                android:centerY="1.0"
                android:endColor="@color/lightWhite"
                android:angle="270"
                />
            <stroke
                android:width="@dimen/progressbar.border.single_border"
                android:color="@color/lightGray" />
            <corners android:radius="@dimen/progressbar.corner.medium_radius" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/green"
                    android:centerColor="@color/green"
                    android:centerY="1.0"
                    android:endColor="@color/lightWhite"
                    android:angle="270"
                    />
                <stroke
                    android:width="@dimen/progressbar.border.single_border"
                    android:color="@color/lightGray" />
                <corners android:radius="@dimen/progressbar.corner.medium_radius" />
            </shape>
        </clip>
    </item>

</layer-list>